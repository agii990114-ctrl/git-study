# fastapi QueryString

``` py
from fastapi import FastAPI
from fastapi.responses import HTMLResponse

app = FastAPI() #was 서버를 켜기 위한 선언 // 변수에 담아줘야하며 시작하는 부분에 선언

arr = []


@app.get("/", response_class=HTMLResponse)
def root(txt:str =""):
   
    print(f"전역 배열 : {arr}")
    print(f"전달 받은 변수 : {txt}")
    if txt == "":
        return """
        <body>
            <form>
                <input type="text" name="txt" />
                <button type="submit">전송</button>
            </form>
        </body>
    """
    else:
        arr.append(txt)
        html = ""
        for v in arr:
            html += f"<li>{v}</li>"

        return f"""
        <body>
            <ul>
                {html}
            </ul>
            <a href="/">돌아가기</a>
        </body>
    """

```
# post 메소드 데이터 전달

```py
from fastapi import FastAPI, Form
from fastapi.responses import HTMLResponse

app = FastAPI() #was 서버를 켜기 위한 선언 // 변수에 담아줘야하며 시작하는 부분에 선언


@app.get("/")
def root(txt):
  return {"status": True, "txt" : txt}


@app.post("/")
def root(txt: str = Form()):
  return {"status": True, "txt": txt}


@app.get("/view", response_class = HTMLResponse)
def view():
  return """
    <body>
      <form action = "/" method="post">
        <input type = "text" name = "txt" />
        <button type = "submit">요청</button>
      </form>
    </body>

"""

```


## 웹서버

### httpd

### nginx


### iis
- world wide web 서비스

## WAS

### fasfAPI

### django


## DBMS

### ORACLE

### MICROSOFT SQL

### DB2(IBM)

### MYSQL

### MariaDB
