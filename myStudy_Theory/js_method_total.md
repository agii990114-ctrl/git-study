### 📝 자바스크립트 주요 메소드 총정리

---

#### 1. 문자열 (String) 메소드
> 문자열은 원본이 변하지 않으며, 항상 새로운 값을 반환합니다.

| 메소드 | 설명 | 예시 |
| :---: | :--- | :--- |
| `indexOf()` | 특정 문자의 시작 **인덱스** 반환 | `'abc'.indexOf('b')` → `1` |
| `includes()` | 특정 문자 **포함 여부** 확인 | `'hello'.includes('he')` → `true` |
| `slice(s, e)` | `s`인덱스부터 `e` **직전**까지 추출 | `'javascript'.slice(0, 4)` → `'java'` |
| `substring()` | `slice`와 유사 (음수 인덱스 사용 불가) | `'hello'.substring(1, 3)` → `'el'` |
| `replace()` | 첫 번째 일치 문자를 **교체** | `'apple'.replace('p', 'b')` → `'abple'` |
| `replaceAll()` | 모든 일치 문자를 **교체** | `'apple'.replaceAll('p', 'b')` → `'abble'` |
| `toUpperCase()` | 모든 문자를 **대문자**로 변환 | `'js'.toUpperCase()` → `'JS'` |
| `toLowerCase()` | 모든 문자를 **소문자**로 변환 | `'JS'.toLowerCase()` → `'js'` |
| `trim()` | 양 끝의 **공백 제거** | `' hello '.trim()` → `'hello'` |
| `split()` | 기준 문자로 나누어 **배열 반환** | `'a,b,c'.split(',')` → `['a', 'b', 'c']` |
| `charAt()` | 특정 위치의 **문자** 반환 | `'cat'.charAt(0)` → `'c'` |
| `startsWith()` | 특정 문자로 **시작**하는지 확인 | `'hello'.startsWith('h')` → `true` |
| `endsWith()` | 특정 문자로 **끝**나는지 확인 | `'file.pdf'.endsWith('.pdf')` → `true` |
| `padStart(n, x)` | 길이를 `n`으로 맞추고 **앞**을 `x`로 채움 | `'5'.padStart(2, '0')` → `'05'` |
| `padEnd(n, x)` | 길이를 `n`으로 맞추고 **뒤**를 `x`로 채움 | `'1'.padEnd(3, '0')` → `'100'` |
| `repeat(n)` | 문자열을 `n`번 **반복** | `'!'.repeat(3)` → `'!!!'` |

---

#### 2. 숫자(Number) 및 수학(Math) 메소드

| 메소드 | 설명 | 예시 |
| :---: | :--- | :--- |
| `toFixed(n)` | 소수점 `n`자리까지 반올림 (**문자열** 반환) | `3.1415.toFixed(2)` → `'3.14'` |
| `parseInt()` | 문자열을 **정수**로 변환 | `parseInt('10.5')` → `10` |
| `parseFloat()` | 문자열을 **실수**로 변환 | `parseFloat('10.5')` → `10.5` |
| `Math.abs()` | **절대값** 반환 | `Math.abs(-5)` → `5` |
| `Math.round()` | **반올림** | `Math.round(4.6)` → `5` |
| `Math.floor()` | **내림** (소수점 버림) | `Math.floor(4.9)` → `4` |
| `Math.ceil()` | **올림** | `Math.ceil(4.1)` → `5` |
| `Math.random()` | 0~1 사이의 **무작위 난수** 발생 | `Math.random()` |
| `Math.max/min` | 인자 중 **최대/최소값** 반환 | `Math.max(1, 3, 2)` → `3` |

---

#### 3. 배열 (Array) 메소드
> `*` 표시가 된 메소드는 **원본 배열을 직접 수정**하므로 주의하세요!

| 메소드 | 설명 | 예시 |
| :---: | :--- | :--- |
| `push()` * | 배열 **끝에 추가** | `[1].push(2)` → `[1, 2]` |
| `pop()` * | 배열 **끝 요소 제거** 및 반환 | `[1, 2].pop()` → `2` |
| `unshift()` * | 배열 **앞에 추가** | `[1].unshift(0)` → `[0, 1]` |
| `shift()` * | 배열 **앞 요소 제거** 및 반환 | `[0, 1].shift()` → `0` |
| `join()` | 요소를 문자로 합쳐서 반환 | `['a','b'].join('-')` → `'a-b'` |
| `reverse()` * | 순서를 **반대로 뒤집음** | `[1, 2].reverse()` → `[2, 1]` |
| `sort()` * | 요소를 **정렬** | `[2, 1].sort()` → `[1, 2]` |
| `forEach()` | 각 요소를 순회하며 함수 실행 | `arr.forEach(v => console.log(v))` |
| `map()` | 요소를 가공해 **새 배열** 반환 | `[1, 2].map(x => x*2)` → `[2, 4]` |
| `filter()` | 조건에 맞는 요소만 추려 **새 배열** 반환 | `[1, 5].filter(x => x > 2)` → `[5]` |
| `find()` | 조건에 맞는 **첫 번째 요소** 반환 | `[10, 20].find(x => x > 15)` → `20` |
| `findIndex()` | 조건에 맞는 **첫 요소의 인덱스** 반환 | `[10, 20].findIndex(x => x > 15)` → `1` |
| `some()` | **하나라도** 조건을 만족하면 `true` | `[1, 2].some(x => x > 1)` → `true` |
| `every()` | **모든** 요소가 만족해야 `true` | `[1, 2].every(x => x > 0)` → `true` |
| `reduce()` | 배열을 **하나의 값으로 축소** (합계 등) | `[1, 2, 3].reduce((a, b) => a + b)` → `6` |

---

#### 4. 객체 (Object) 메소드

| 메소드 | 설명 | 예시 |
| :---: | :--- | :--- |
| `Object.keys()` | 객체의 **키(Key)**를 배열로 반환 | `Object.keys({a:1})` → `['a']` |
| `Object.values()` | 객체의 **값(Value)**을 배열로 반환 | `Object.values({a:1})` → `[1]` |
| `Object.entries()` | **[키, 값] 쌍**의 배열을 반환 | `Object.entries({a:1})` → `[['a', 1]]` |
| `Object.assign()` | 객체들을 **하나로 병합** | `Object.assign({a:1}, {b:2})` |
