# 알게된 내용

- max-width는 width와 함께 사용가능하며, 이때의 width는 기본 너비를 설정하는 역할
- margin은 'block'요소에만 적용이 가능하며 'inline-block'요소에는 적용이 안됨

# Bootstrap (UI 프레임 워크)

--Bootstrap 사이트나 W3 사이트에서 주소를 복사해 'head'태그 영역 안에 붙여 넣는다.
