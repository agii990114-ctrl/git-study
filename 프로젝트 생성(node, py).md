## node
1. 폴더 생성
2. `npm init -y` 으로 프로젝트 초기화(생성)
3. package.json 파일에서 type을 module로 변경
4. src 폴더 생성 후 node.js 파일 생성 및 이동
5. package.json 파일의 `script` 영역에서 명령어와 경로 설정
6. 터미널을 통해 `npm i commander` 를 설치

## python
1. 폴더 생성
2. `uv init .`으로 프로젝트 초기화(생성)
3. src 폴더 생성 후 py 파일 생성 및 이동
